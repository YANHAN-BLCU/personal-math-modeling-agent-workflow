# 数学建模 Codex 项目使用指南

本项目用于题目理解、模型设计、代码求解、科研制图和论文草稿生成。生成内容不能原样作为参赛作品提交；参赛队伍必须独立核验数值、公式、约束、图表、引用和当届官方格式，并进行人工改写。

## 快速开始

```powershell
.\scripts\doctor.ps1
.\scripts\init_project.ps1 -Name "2026-CUMCM-A"
```

需要从零重建环境时，依次运行 `scripts/install_algorithms.ps1` 和 `scripts/install_connectors.ps1`。

随后在 Codex 中说明题目目录、竞赛名称、届次和目标交付物。完整流程默认依次执行建模、编程和论文阶段，并经过 M1、P1、P2、W1、W2 独立验收。

## 交付物边界

- `题目分析报告.md`、`术语表格.md`：建模草稿。
- 代码、结果表和图表：必须实际运行并由人工复核。
- `完整论文.docx` 及可选 LaTeX/PDF：论文草稿，必须按当届官方规则修改。
- `results/复现清单.json`：记录输入哈希、环境、随机种子、参数和复现命令。
